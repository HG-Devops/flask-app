version: '3'

services:
  web:
    build: .
    ports:
      - "80:5000"
    depends_on:
      - db
    env_file:
      - .env

  db:
    image: mysql:5.7
    env_file:
      - .env
    volumes:
      - mysql-data:/var/lib/mysql

volumes:
  mysql-data:
